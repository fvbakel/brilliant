create table pages (
    -- `offset` int(8) NOT NULL DEFAULT 0,
    `page_id` int(11) DEFAULT 0,
    `title` varbinary(255) NOT NULL DEFAULT ''
);
.separator :
.import pagelinks-clean.txt pages
create index i_pages 
on pages (page_id)
;