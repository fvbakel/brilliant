# Rollup test 001

This directory contains a test of rollup with threejs and a small gui.

To run:

```sh
npm install
npm run build
npm start
```

To run the rest cases:

```sh
npm install
npm run build
npm test
```